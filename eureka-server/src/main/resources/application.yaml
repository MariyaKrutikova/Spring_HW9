server:
  port: 8760
spring:
  application:
    name: eserver

eureka:
  client:
    register-with-eureka: false
    fetch-registry: false
    service-url:
      defaultZone: http://localhost:8760/eureka

